metrics:
  counters:
    - name: python_module_imports_total
      help: Number of python:import__find__load__start USDT calls per module
      labels:
        - name: module
          size: 128
          decoders:
            - name: string
